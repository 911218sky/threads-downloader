name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            asset_name: windows-x64
            ext: .exe
          - os: macos-latest
            asset_name: macos-x64
            ext: ""
          - os: ubuntu-latest
            asset_name: linux-x64
            ext: ""

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller
          pip install -e .

      - name: Get version
        id: version
        shell: bash
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Build executables (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          pyinstaller --onefile --noupx --name threads-downloader-cli --exclude-module tkinter --exclude-module matplotlib --exclude-module numpy --exclude-module pandas --exclude-module PIL --exclude-module scipy --exclude-module pytest --exclude-module unittest --paths src -c -y src/cli_entry.py
          pyinstaller --onefile --noupx --name threads-downloader --exclude-module tkinter --exclude-module matplotlib --exclude-module numpy --exclude-module pandas --exclude-module PIL --exclude-module scipy --exclude-module pytest --exclude-module unittest --paths src -c -y src/main.py

      - name: Build executables (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          pyinstaller --onefile --strip --noupx --name threads-downloader-cli --exclude-module tkinter --exclude-module matplotlib --exclude-module numpy --exclude-module pandas --exclude-module PIL --exclude-module scipy --exclude-module pytest --exclude-module unittest --paths src -c -y src/cli_entry.py
          pyinstaller --onefile --strip --noupx --name threads-downloader --exclude-module tkinter --exclude-module matplotlib --exclude-module numpy --exclude-module pandas --exclude-module PIL --exclude-module scipy --exclude-module pytest --exclude-module unittest --paths src -c -y src/main.py

      - name: Compress with UPX (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          if command -v upx &> /dev/null; then
            upx --best --lzma dist/threads-downloader-cli dist/threads-downloader || true
          fi

      - name: Rename artifacts
        shell: bash
        run: |
          mkdir -p output
          VERSION="${{ steps.version.outputs.VERSION }}"
          EXT="${{ matrix.ext }}"
          PLATFORM="${{ matrix.asset_name }}"
          
          mv "dist/threads-downloader-cli${EXT}" "output/threads-downloader-cli-${VERSION}-${PLATFORM}${EXT}"
          mv "dist/threads-downloader${EXT}" "output/threads-downloader-${VERSION}-${PLATFORM}${EXT}"

      - name: Test executables (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          chmod +x ./output/threads-downloader-cli-${{ steps.version.outputs.VERSION }}-${{ matrix.asset_name }}
          ./output/threads-downloader-cli-${{ steps.version.outputs.VERSION }}-${{ matrix.asset_name }} --help
          echo "Tests passed!"

      - name: Test executables (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          & "./output/threads-downloader-cli-${{ steps.version.outputs.VERSION }}-${{ matrix.asset_name }}.exe" --help
          Write-Host "Tests passed!"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: threads-downloader-${{ matrix.asset_name }}
          path: output/*

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          mkdir -p release
          find artifacts -type f -exec cp {} release/ \;
          ls -la release/

      - name: Generate release notes
        run: |
          sed 's/\${VERSION}/${{ steps.version.outputs.VERSION }}/g' .github/RELEASE_TEMPLATE.md > release_notes.md

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: Threads Downloader v${{ steps.version.outputs.VERSION }}
          files: release/*
          body_path: release_notes.md
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
